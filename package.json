{
  "name": "sangpoint-referral-chain",
  "version": "1.0.0",
  "description": "Referral system for SangPoint where users can share a link to earn rewards.",
  "main": "index.html",
  "scripts": {
    "start": "lite-server",
    "build": "npm run build",  // If you use a build process, for example using Webpack or Parcel
    "deploy": "vercel deploy"  // If you're deploying to Vercel
  },
  "author": "Peter Ngeno",
  "license": "MIT",
  "dependencies": {
    "lite-server": "^2.5.4"  // A simple web server for local development
  },
  "devDependencies": {
    "webpack": "^5.50.0",  // If you plan on using Webpack for bundling JavaScript
    "webpack-cli": "^4.8.0"
  }
}
